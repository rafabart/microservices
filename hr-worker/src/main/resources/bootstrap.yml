spring:
  application:
    name: hr-worker

#  profile:
#    active: test

  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'