server:
  port: 8001

spring:
  application:
    name: hr-worker

  datasource:
    url: jdbc:h2:mem:testdbworker
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

